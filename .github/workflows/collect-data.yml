name: Collect dolar data

on:
  schedule:
    - cron: '*/5 13-18 * * 1-5'  # Every 5 min, Mon-Fri, 8:00 AM - 1:30 PM COT (UTC-5 = 13:00-18:30 UTC)
  workflow_dispatch: # Allow manual trigger from GitHub UI

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Call collect-data endpoint
        run: |
          RESPONSE=$(curl -sf -X POST "${{ vars.APP_URL }}/api/cron/collect-data" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json")

          echo "$RESPONSE" | python3 -m json.tool > formatted.json

          echo "## Collect data response" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          cat formatted.json >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
